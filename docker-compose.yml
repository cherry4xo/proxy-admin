version: "3.9"

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./proxy.db:/app/proxy.db
      - ./scripts:/app/scripts:ro
    environment:
      - PYTHONUNBUFFERED=1
